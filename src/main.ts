import * as metaData from './input.json'

export const traverseMetaData = (inputMetaData: Record<string, unknown>): Array<Record<string, unknown>> => {
  // Include the logic here
  return []
}

traverseMetaData(metaData)
